// doesn't support custom keywords (instanceof, typeof)
// which is why no runtime benchmarks are included (wouldn't be a fair comparison)
import { validate } from "../compiled/product.compiled.mjs";

validate({});
